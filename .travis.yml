sudo: required
services:
  - docker

after_success:
  - docker build -t dpowersnewg/aspcorenetapp ./AspNetCoreApp
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push dpowersnewg/aspcorenetapp